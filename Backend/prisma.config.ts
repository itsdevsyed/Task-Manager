// import { PrismaClient } from "@prisma/client";

// // Local SQLite — just pass an empty object
// // Prisma 7 reads the datasource from schema automatically
// const prisma = new PrismaClient({});

// export default prisma;
// export { prisma };
